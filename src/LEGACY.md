# Inactive experiments

The earlier drawer/sidebar experiment remains in `src/renderer/` and
`src/whatsapp/` for reference. It is not imported by the PrettyZap foundation,
and those directories are excluded from the active TypeScript build.
